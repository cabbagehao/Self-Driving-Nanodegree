不加启发函数，9000多步
启发函数：
	x+y - (goal[x]+goal[y]) ，	需要8000多步
	(x-goal[x])^2 + (y-goal[y])^2，	只需要600多步。
	将左右转弯更改为正负20°， 	需要427步
	将探索的方向粒度从5°改为8°， 	需要268步
				// 上面两种优化牺牲了特殊场景下的鲁棒性。
	
还可以从theta角度优化启发函数。 计算state角度与目标夹角。 如果>90则加上大权值
